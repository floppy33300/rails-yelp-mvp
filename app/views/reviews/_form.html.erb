<div class="container">
  <%= simple_form_for [@restaurant, @review] do |r| %>
    <%= r.input :content %>
    <%= r.input :rating, collection: Review::RATINGS %>
    <%= r.submit "add a review", class: "btn btn-primary w-100" %>
  <% end %>
</div>
